# Native UI, Settings, And Lobby Rewrite Archive

These files document the completed MultiplayerTools rewrite that split the native UI helper layer, staged the settings menu, and extracted lobby UI behavior into focused feature classes.

Active architecture decisions remain in `docs/adr/`.
