<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>NameFuzzTest</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Compile the REAL matcher straight from the mod — no copy. Keep this in lockstep with the mod build. -->
    <Compile Include="../../LobbyKit/Utils/NameMatch.cs" Link="NameMatch.cs" />
  </ItemGroup>
</Project>
